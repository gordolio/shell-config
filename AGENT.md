# Shell Config

## Claude Code Statusline (`claude/`)

The `claude/` directory contains the statusline script for Claude Code.

- `statusline-command.sh` — Claude Code statusline script that displays git info, battery, token usage %, version, and vim mode. Uses data from Claude Code's statusline JSON input (no external API calls needed).
